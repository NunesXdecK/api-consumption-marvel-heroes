import { styled } from "styled-components";
import { Content as cc } from "../../../../components/ui/paper/styles";

export const Content = styled(cc)`
  padding: 0;
  display: block;
  overflow: hidden;
`